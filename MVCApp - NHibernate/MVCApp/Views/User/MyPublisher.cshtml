@model IEnumerable<MVCApp.Models.MyPublisherExt>
@{
    ViewBag.Title = "MyPublisher";
}

<h2>MyPublisher</h2>
<script type="text/javascript">

    $(document).ready(function () {

        var modelData = @Html.Raw(Json.Encode(Model))
        $("#grid").kendoGrid({
            
              pageable: true
            , sortable: true
            , silectable: true
            , selectable: true
            , columns: [
            { field: "publisher.Id", title: "Id" }
         , { field: "publisher.ReplyToAddress", title: "ReplyToAddress" }
         , { field: "publisher.Headers", title: "Headers" }
         , { field: "reader", title: "Body" }
            ]
             , dataSource: { pageSize: 10, data:modelData }

        });
    });


 
</script>

<table id="grid">
</table>
